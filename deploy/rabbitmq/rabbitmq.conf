# Listeners: TCP (PLAIN, compat) + TLS (PLAIN + EXTERNAL via cert CN).
listeners.tcp.default            = 5672
listeners.ssl.default            = 5671

ssl_options.cacertfile           = /etc/rabbitmq/tls/ca.crt
ssl_options.certfile             = /etc/rabbitmq/tls/server.crt
ssl_options.keyfile              = /etc/rabbitmq/tls/server.key
ssl_options.verify               = verify_peer
ssl_options.fail_if_no_peer_cert = true

# CN do cert vira user RabbitMQ (precisa bater com agent-<nome> criado por provision_agent.sh).
auth_mechanisms.1                = PLAIN
auth_mechanisms.2                = EXTERNAL
ssl_cert_login_from              = common_name
