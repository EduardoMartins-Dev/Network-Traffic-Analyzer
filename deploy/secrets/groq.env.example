# groq.env — credencial da Groq Cloud API (somente quando NARRATOR_BACKEND=groq).
# Copie para `groq.env` (no mesmo diretório) e preencha os valores reais.
# `groq.env` está no .gitignore.

GROQ_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
GROQ_MODEL=llama-3.3-70b-versatile
